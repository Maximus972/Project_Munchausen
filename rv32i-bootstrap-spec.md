# Ассемблер с нуля: RV32I bootstrap

Спецификация учебного проекта. Версия 1.1 — журнал изменений в §14.

---

## 1. Цель и критерий готовности

**Цель.** Пройти путь от голого машинного кода до ассемблера с раздельной компиляцией, не используя ни одного готового транслятора. Понять ассемблер и линкер изнутри, построив их.

**Критерий готовности (фикспоинт).** Проект считается завершённым, когда выполняется:

```
asm(asm.s) → asm.o      побайтово равен предыдущему asm.o
ld(asm.o, lib.o) → asm  побайтово равен предыдущему asm
```

То есть ассемблер, написанный на собственном языке, собирает сам себя, и собственный линкер линкует сам себя. Это единственная проверка, которую невозможно подделать.

**Не является целью.** Оптимизация, макросы, поддержка расширений (M, A, F, D, C), динамическая линковка, разделяемые библиотеки, отладочная информация DWARF.

---

## 2. Платформа и окружение

| Параметр | Значение |
|---|---|
| Хост | Linux CachyOS, x86-64 |
| Целевая ISA | RV32I, base integer, без расширений |
| ABI | ilp32 (soft-float) |
| Исполнение | `qemu-riscv32` (user-mode emulation) |
| Формат исполняемого файла | ELF32, `ET_EXEC`, статический, без libc |
| Формат объектного файла | ELF32, `ET_REL` |
| Интерфейс к ОС | прямые syscall через `ecall` |

### 2.1 Смоук-тест нулевого дня

Прежде чем писать хоть байт, проверить, что окружение живо:

```sh
qemu-riscv32 --version
llvm-objdump --version     # или riscv64-linux-gnu-objdump
```

Если `qemu-riscv32` отсутствует — на Arch/CachyOS он лежит в пакете `qemu-emulators-full` (или `qemu-user`).

**Результат смоук-теста (2026-09-04).** `qemu-riscv32 11.1.1`, `llvm-objdump` / `llvm-readelf` 22.1.8, `gdb 17.2` — на месте. RV32 user-mode проверен четырьмя пробными ELF, собранными в обход ступеней (диагностика окружения, не сборка): код выхода доходит до шелла, `argc` и `argv[1]` читаются со стека по описанной раскладке, `write` в stdout работает, bss-страница за концом файла доступна на запись и обнулена.

**Решение по M0: остаёмся на RV32I.** Резервный план RV64 не активируется.

**Чего в системе нет: интерактивного hex-редактора.** Есть только `xxd`, а он запрещён §3 как конвертер. Поставить до первого байта M0 — `hexedit` (минималистичный), `bvi` (vi-подобный, удобнее для длинных правок) или `ghex` (GUI). Без него ступень S0 буквально нечем набивать: это единственный разрешённый инструмент ввода первых байтов.

**Резервный план.** Linux-userspace на RV32 менее обкатан, чем на RV64. Если `qemu-riscv32` поведёт себя странно на самых первых шагах (не запускается корректный ELF, syscall не возвращают ожидаемого), переключиться на **RV64I / `qemu-riscv64`**. Дельта невелика и локализована:

- ELF64 вместо ELF32 (заголовок 64 байта, Phdr 56 байт, другой порядок полей).
- Появляются W-суффиксные инструкции (`addiw`, `slliw`, …) и `ld`/`sd`.
- Указатели 8 байт — меняются шаги в таблицах и размеры полей.
- Кодировка инструкций и форматы **не меняются вообще**.

Решение принимается на вехе M0 и дальше не пересматривается.

---

## 3. Правила чистоты

### Запрещено на всём протяжении проекта

- Любой ассемблер, компилятор, транслятор, кроме собственных ступеней.
- Любые утилиты конвертации hex → binary (`xxd -r`, `printf`, `perl`, `python`) — это и есть ступень S0, её нельзя пропускать.
- Копирование чужого исходного кода ступеней.
- libc, crt-объекты, линкер `ld` из binutils как часть сборки.

### Разрешено

- **Hex-редактор** для набивки байтов ступени S0 руками. Это единственный способ, которым первые байты попадают в файл.
- **Текстовые файлы** любого вида как *документация*: coding sheets, таблицы кодировок, заметки. Их не читает ни одна программа.
- **Отладочные «глаза», не участвующие в сборке**: `llvm-objdump`, `llvm-readelf`, `qemu-riscv32 -strace`, `qemu-riscv32 -d in_asm`, `gdb`, `cmp`, `hexdump` для просмотра.
- **Системный `ld` и `objdump` как эталон для сверки** — линковать свои `.o` чужим линкером и сравнивать с результатом своего. Это диагностика, а не сборка.
- Официальные спецификации: *RISC-V Unprivileged ISA*, *RISC-V ELF psABI*, `asm-generic/unistd.h`.

### Формулировка принципа

Чистота относится к **машине**, а не к человеку. Программист 1960-х заполнял бумажный coding sheet, считал смещения карандашом и набивал итоговые байты руками. Никакая программа этот листок не читала. Разрешено всё, что находится на стороне листка.

---

## 4. Лестница бутстрапа

| Ступень | Что делает | На чём написана | Ориентир по объёму |
|---|---|---|---|
| **S0** `hex0` | hex-текст → байты; `#` — комментарий | машинный код, hex-редактор руками | 400–700 байт |
| **S1** `hex2` | + метки и три вида ссылок | формат S0 | ~1.5–3 КБ |
| **S2** `asm1` | мнемоники, регистры, директивы; выход — плоский `ET_EXEC` | формат S1 | ~6–10 КБ |
| **S3** `asm2` | то же + секции, `.globl`, выход — `ET_REL` с релокациями | язык S2 | ~10–15 КБ |
| **S4** `ld` | линкер: слияние секций, глобальная таблица символов, релокации | язык S3 | ~6–10 КБ |
| **S5** | фикспоинт: `asm2` и `ld` пересобирают себя байт-в-байт | свой язык | — |

Обратите внимание: **S1 — это уже линкер в миниатюре** (таблица символов, список патчей, разрешение forward-ссылок). S4 отличается только тем, что работает с несколькими входными файлами. Вы напишете эту логику дважды, и второй раз будет осознанным.

---

## 5. Целевой формат: ELF32 для RISC-V

### 5.1 Минимальный исполняемый файл

52 байта ELF-заголовка + 32 байта одного `PT_LOAD` = 84 байта (0x54). Базовый адрес `0x10000`, точка входа `0x10054`.

```
off  байты                          поле
00   7F 45 4C 46                    magic
04   01                             EI_CLASS = ELFCLASS32
05   01                             EI_DATA  = little-endian
06   01                             EI_VERSION
07   00 00 00 00 00 00 00 00 00     EI_OSABI + padding
10   02 00                          e_type    = ET_EXEC
12   F3 00                          e_machine = EM_RISCV (243)
14   01 00 00 00                    e_version
18   54 00 01 00                    e_entry   = 0x00010054
1C   34 00 00 00                    e_phoff   = 52
20   00 00 00 00                    e_shoff   = 0
24   00 00 00 00                    e_flags   = 0 (RV32I soft-float)
28   34 00                          e_ehsize     = 52
2A   20 00                          e_phentsize  = 32
2C   01 00                          e_phnum      = 1
2E   28 00                          e_shentsize  = 40
30   00 00                          e_shnum      = 0
32   00 00                          e_shstrndx   = 0
--- Elf32_Phdr ---
34   01 00 00 00                    p_type   = PT_LOAD
38   00 00 00 00                    p_offset = 0
3C   00 00 01 00                    p_vaddr  = 0x10000
40   00 00 01 00                    p_paddr  = 0x10000
44   00 10 00 00                    p_filesz = 0x1000
48   00 20 00 00                    p_memsz  = 0x2000
4C   07 00 00 00                    p_flags  = RWX
50   00 10 00 00                    p_align  = 0x1000
54   <код>
```

**Гоча:** в `Elf32_Phdr` поле `p_flags` стоит **последним перед `p_align`**, а в `Elf64_Phdr` — сразу после `p_type`. Порядок полей разный. Это классический источник суток отладки.

Раскладка выше — минимальный ELF для вехи M0, на нём удобно разобраться с полями. **Рабочая схема проекта другая: два `PT_LOAD`, точка входа `0x10074`** — почему именно так, объясняет §5.2, итоговая карта памяти в §5.3.

### 5.2 Размер файла, bss и почему «добить нулями» — вредный совет

Первое, что нужно знать про загрузчик: **`p_filesz` можно объявить больше фактического размера файла, но только в пределах округления этого размера вверх до страницы.** Ядро (и загрузчик qemu) отображает сегмент страницами; хвост последней страницы за концом файла ядро само обнуляет, а вот страница, целиком лежащая за EOF, при обращении даёт SIGBUS.

Проверено четырьмя пробными ELF на этой машине:

| Вариант | Что объявлено | Файл | Результат |
|---|---|---|---|
| bss на второй странице | `filesz=0x1000`, `memsz=0x2000`, запись по `0x11000` | 108 байт, **не добит нулями** | работает, `exit=42` |
| дальний конец bss | то же, запись по `0x11FFF` | 112 байт | работает |
| завышенный filesz | `filesz=0x8000`, запись по `0x14000` | 108 байт | **SIGBUS**, код выхода 135 |
| два `PT_LOAD` | код + отдельный bss-сегмент `filesz=0` | 140 байт | работает |

Отсюда три вывода.

**Добивать файл нулями не нужно и вредно.** Хвост последней страницы и так нулевой — это видно из первого варианта: файл 108 байт, а память до `0x10FFF` читается и пишется. Но главное не в экономии: `hex0` обязан собирать сам себя, то есть его исходник `hex0.hex` содержит **все** байты выходного файла. Добивка до 4096 означает 3–3.5 тысячи нулевых байтов, то есть 7 тысяч hex-цифр, набитых руками. Приём, поданный как «снимающий боль», убивает главное свойство ступени S0.

**Правило для `p_filesz`.** Объявляйте `p_filesz = 0x1000`, пока файл меньше 4096 байт; `0x2000`, пока меньше 8192; и так далее. Это единственная константа, требующая внимания при росте кода, и трогать её приходится раз в 4 КБ. Завысить сильнее нельзя — третья строка таблицы показывает цену ошибки, и «программа падает без единого сообщения» отлаживается долго, если не знать причину заранее.

**Данные — отдельным сегментом.** Схема «`memsz` больше `filesz`, bss сразу за кодом» привязывает адреса данных к размеру кода: вырос код за 4 КБ — и вся карта памяти уехала, а вместе с ней все вручную вписанные в S0/S1 константы `lui`/`addi`. Вместо этого объявите **второй `PT_LOAD` с `p_filesz = 0`**: он не читает из файла ничего, ядро отдаёт чистую анонимную память по фиксированному адресу. Цена — 32 байта заголовка, `e_phnum = 2` и точка входа `0x10074` вместо `0x10054`. Выгода — адреса данных не меняются больше никогда, ни на одной ступени.

### 5.3 Карта памяти

Одна на все ступени S0–S4. Зафиксировать сейчас, дальше не пересматривать — на неё завязаны константы, вписанные в S0 и S1 руками.

```
сегмент 1  R+X   vaddr 0x10000  offset 0  filesz = размер файла, округлённый до 4 КБ
сегмент 2  R+W   vaddr 0x18000  filesz 0  memsz 0x8000     (0x18000…0x1FFFF)
```

Раскладка второго сегмента:

```
0x18000  скаляры: счётчики таблиц, fd, позиции, флаги     256 байт
0x18100  входной буфер                                     ~4 КБ
0x19000  выходной буфер                                     4 КБ
0x1A000  строковый пул: имена символов, через \0            8 КБ
0x1C000  таблица символов, 16 байт на запись               8 КБ  (512 символов)
0x1E000  таблица патчей,   12 байт на запись               8 КБ  (~680 патчей)
```

Запись таблицы символов: `имя` (смещение в пуле), `значение`, `секция/флаги`, резерв. Запись патча: `адрес`, `индекс символа`, `тип ссылки`.

**Лимиты обязаны быть явными.** У каждой таблицы есть счётчик и потолок; при переполнении — сообщение в stderr и `exit(2)`. Молчаливая запись за границу таблицы в проекте без MMU-защиты между областями даёт порчу соседней структуры и отладку, которая занимает дни. Проверить лимиты нужно один раз, при первой же ступени, которая эти таблицы заводит (S1).

### 5.4 Syscall ABI

Номер в `a7`, аргументы в `a0`–`a5`, инструкция `ecall`, результат в `a0`. Отрицательный `a0` — ошибка (`-errno`).

| № | Вызов | Аргументы |
|---|---|---|
| 56 | `openat` | `a0`=AT_FDCWD, `a1`=путь, `a2`=flags, `a3`=mode |
| 57 | `close` | `a0`=fd |
| 63 | `read` | `a0`=fd, `a1`=буфер, `a2`=длина |
| 64 | `write` | `a0`=fd, `a1`=буфер, `a2`=длина |
| 93 | `exit` | `a0`=код |

**Важно:** в generic-таблице (которую использует RISC-V) **нет `open`**, только `openat`. `AT_FDCWD = -100 = 0xFFFFFF9C`.

Константы: `O_RDONLY = 0`; для выходного файла `O_WRONLY|O_CREAT|O_TRUNC = 1|64|512 = 577 = 0x241`, `mode = 0644 = 0x1A4`.

**Стек при входе:** `sp` указывает на `argc`, далее подряд `argv[0]`, `argv[1]`, … (слова по 4 байта). То есть имя входного файла лежит по адресу `[sp+8]`, выходного — `[sp+12]`.

---

## 6. Кодировка RV32I

Все инструкции ровно 32 бита, в файле — little-endian. Общие поля:

```
opcode  [6:0]     rd  [11:7]    funct3 [14:12]
rs1     [19:15]   rs2 [24:20]   funct7 [31:25]
```

### 6.1 Шесть форматов

| Формат | Раскладка битов 31…0 | Примеры |
|---|---|---|
| **R** | `funct7[31:25] rs2 rs1 funct3 rd opcode` | `add`, `sub`, `and`, `sll` |
| **I** | `imm[11:0][31:20] rs1 funct3 rd opcode` | `addi`, `lw`, `jalr`, `ecall` |
| **S** | `imm[11:5] rs2 rs1 funct3 imm[4:0] opcode` | `sw`, `sb` |
| **B** | `imm[12] imm[10:5] rs2 rs1 funct3 imm[4:1] imm[11] opcode` | `beq`, `bne` |
| **U** | `imm[31:12][31:12] rd opcode` | `lui`, `auipc` |
| **J** | `imm[20] imm[10:1] imm[11] imm[19:12] rd opcode` | `jal` |

Форматы **B** и **J** — единственное по-настоящему неприятное место ISA: их непосредственные значения разбросаны по битам не по порядку. Младший бит смещения всегда 0 и не хранится. Диапазоны: B — ±4 КиБ, J — ±1 МиБ.

### 6.2 Таблица опкодов

| Группа | opcode | Инструкции (funct3) |
|---|---|---|
| OP-IMM | `0010011` | `addi`=000, `slti`=010, `sltiu`=011, `xori`=100, `ori`=110, `andi`=111, `slli`=001, `srli`/`srai`=101 |
| OP | `0110011` | `add`/`sub`=000, `sll`=001, `slt`=010, `sltu`=011, `xor`=100, `srl`/`sra`=101, `or`=110, `and`=111 |
| LOAD | `0000011` | `lb`=000, `lh`=001, `lw`=010, `lbu`=100, `lhu`=101 |
| STORE | `0100011` | `sb`=000, `sh`=001, `sw`=010 |
| BRANCH | `1100011` | `beq`=000, `bne`=001, `blt`=100, `bge`=101, `bltu`=110, `bgeu`=111 |
| JALR | `1100111` | funct3=000 |
| JAL | `1101111` | — (J-формат) |
| LUI | `0110111` | — (U-формат) |
| AUIPC | `0010111` | — (U-формат) |
| SYSTEM | `1110011` | `ecall` (всё остальное = 0) |

`sub` и `sra` отличаются от `add` и `srl` только `funct7 = 0100000` (у остальных `0000000`). У `slli`/`srli`/`srai` в поле `rs2` лежит `shamt`, а `funct7` играет ту же роль.

### 6.3 Регистры

| x | ABI | Роль |
|---|---|---|
| x0 | `zero` | всегда 0, запись игнорируется |
| x1 | `ra` | адрес возврата |
| x2 | `sp` | стек |
| x5–x7 | `t0`–`t2` | временные |
| x8–x9 | `s0`/`fp`, `s1` | сохраняемые |
| x10–x17 | `a0`–`a7` | аргументы / возврат / номер syscall в `a7` |
| x18–x27 | `s2`–`s11` | сохраняемые |
| x28–x31 | `t3`–`t6` | временные |

### 6.4 Опорные кодировки для сверки

```
nop            addi x0, x0, 0       0x00000013
ret            jalr x0, 0(x1)       0x00008067
ecall                               0x00000073
li a0, 42      addi x10, x0, 42     0x02A00513   → байты 13 05 A0 02
li a7, 93      addi x17, x0, 93     0x05D00893   → байты 93 08 D0 05
lw a0, 0(sp)                        0x00012503
sb a0, 0(a1)                        0x00A58023
```

### 6.5 Подмножество для S0

Ступени S0 достаточно примерно двадцати инструкций: `addi`, `lui`, `add`, `sub`, `and`, `or`, `xor`, `andi`, `ori`, `slli`, `srli`, `lb`, `lbu`, `lw`, `sb`, `sw`, `beq`, `bne`, `blt`, `bltu`, `bge`, `bgeu`, `jal`, `jalr`, `ecall`. Всё остальное — роскошь.

### 6.6 Дисциплина ручной набивки

- **Резерв из `nop`** (`13 00 00 00`) между логическими блоками. Вставка инструкций съедает резерв, а не сдвигает весь файл.
- **Все данные — в bss-хвосте** по фиксированным адресам. Рост кода не двигает данные.
- **Coding sheet** обязателен. Формат: `адрес | 4 байта | мнемоника | комментарий`. Это ваш единственный «исходник» для S0.

Приятная особенность RISC-V: длина инструкции фиксирована, поэтому вставка одной инструкции сдвигает адреса ровно на 4, а не на непредсказуемое число. Пересчёт смещений становится арифметикой, а не расследованием.

---

## 7. Спецификации ступеней

### S0 — `hex0`

**Вызов:** `qemu-riscv32 ./hex0 input.hex output.bin`

**Входной язык:**
- Шестнадцатеричные цифры, обе регистры (`0-9A-Fa-f`). Каждые две цифры — один байт.
- Пробел, табуляция, перевод строки, `\r` — игнорируются, в том числе внутри пары цифр.
- `#` и `;` — комментарий до конца строки.
- Любой другой символ — прекратить работу с ненулевым кодом.

**Поведение:** читать по одному байту через `read` (буферизация не нужна), писать по одному байту через `write` (медленно, но корректно). Обработки ошибок нет вообще: любая неудача — `exit(1)`.

**Ключевое свойство:** `hex0` собирает сам себя. Его собственный исходник — это ELF-заголовок и код, записанные в его же hex-формате с комментариями. Первый фикспоинт проекта.

### S1 — `hex2`

**Вызов:** `qemu-riscv32 ./hex2 input.hex2 output.bin`

**Добавляет к языку S0:**

| Синтаксис | Смысл |
|---|---|
| `:name` | определение метки в текущей позиции |
| `~name` | ссылка J-типа: пропатчить последнее слово как `jal` |
| `@name` | ссылка B-типа: пропатчить последнее слово как ветвление |
| `&name` | абсолютный 32-битный адрес, кладётся как слово данных |

Ровно три вида ссылок, и все три соответствуют реальным форматам ISA — патчер обязан уметь раскладывать смещение по разбросанным битам B и J. Это и есть основное учебное содержание ступени.

**Алгоритм:** два прохода. Первый — считать длины, собрать таблицу меток. Второй — эмитировать байты, ссылки разрешать по таблице. Forward-ссылки работают за счёт первого прохода.

**Сознательное упрощение:** доступ к глобальным данным на этой ступени делается парой `lui`/`addi` с **вручную вписанными константами**, потому что все данные лежат в bss по фиксированным адресам, которые вы контролируете. Релокации `%hi`/`%lo` появятся только на S2.

### S2 — `asm1`

Первый настоящий ассемблер. Пишется в формате S1.

- Мнемоники всех инструкций RV32I, регистры по ABI-именам и по `xN`.
- Псевдоинструкции: `li`, `la`, `mv`, `j`, `call`, `ret`, `nop`, `beqz`, `bnez`, `bgt`, `ble` (последние две — обмен операндов у `blt`/`bge`).
- Директивы: `.text`, `.data`, `.bss`, `.byte`, `.half`, `.word`, `.ascii`, `.asciz`, `.space`, `.align`, `.equ`.
- `%hi(sym)` / `%lo(sym)` для `lui`+`addi` и `lui`+`sw`. Помнить про **коррекцию `%hi`**: если бит 11 у `%lo` равен 1, `%hi` увеличивается на 1, потому что `addi` знакорасширяет 12-битное поле.
- Выход: плоский `ET_EXEC` с одним `PT_LOAD` (как в §5.1). Никаких секций в файле.

### S3 — `asm2`

- Всё из S2 плюс `.globl`, `.local`, `.extern`.
- Выход: `ET_REL` (см. §8).
- Пишется на языке S2.

### S4 — `ld`

**Вызов:** `qemu-riscv32 ./ld -o prog a.o b.o c.o`

**Алгоритм:**
1. Прочитать все объектники, разобрать заголовки секций.
2. Слить одноимённые секции в порядке следования файлов, запомнив смещение каждого вклада.
3. Разложить `.text`, `.data`, `.bss` по адресам от `0x10000`, с выравниванием на страницу между разными правами доступа.
4. Построить глобальную таблицу символов. Неразрешённый символ — ошибка с внятным сообщением.
5. Применить релокации.
6. Записать `ET_EXEC` с двумя `PT_LOAD`: R+X для `.text`, R+W для `.data`+`.bss`.

Архивы `.a` не нужны — объектники передаются списком. Динамики нет: ни `.dynamic`, ни PLT, ни GOT.

### 7.5 Сквозные соглашения

**Командная строка.** Флаги парсить дорого, поэтому их нет нигде, кроме линкера: `hex0 in out`, `hex2 in out`, `asm1 in out`, `asm2 in out`, `ld -o out a.o b.o …`. Ровно один флаг `-o` и только первым.

**Коды выхода.** `0` — успех, `1` — синтаксическая ошибка во входе, `2` — переполнение таблицы или буфера, `3` — ошибка ввода-вывода (`openat`/`read`/`write` вернули отрицательное), `4` — неразрешённый символ (только `ld`). Разные коды нужны, чтобы регрессионный раннер отличал «программа честно отвергла плохой вход» от «программа сломалась».

**Диагностика — не роскошь, а условие выживания.** У `hex0` её нет по определению (`exit(1)` и всё). Но начиная с **S2** обязательны: имя входного файла, номер строки, короткий код ошибки — в stderr (fd = 2). Без номера строки поиск ошибки в 10-килобайтном исходнике на своём ассемблере превращается в бинарный поиск делением файла пополам. Печать десятичного числа — отдельная подпрограмма (`itoa`), написать её один раз и таскать дальше.

**Правило фиксированной длины.** Ассемблер двухпроходный, значит **размер инструкции не имеет права зависеть от значения, которое станет известно только на втором проходе**. Отсюда:

- `la sym`, `call sym`, `li` с символом — **всегда** две инструкции (`lui`+`addi`, `lui`+`jalr`), даже когда адрес влез бы в 12 бит.
- `li` с числовым литералом — длина по значению: литерал известен на первом проходе.
- Никакого relaxation: ни сжатия пары в одну инструкцию, ни расширения ветвления в `jal`.

**Ветвление дальше ±4 КиБ — ошибка, а не автозамена.** К моменту S3 ваш `.text` перевалит за 4 КиБ, и дальний `beq` перестанет кодироваться. Ассемблер обязан сказать об этом внятно, а вы — переписать место руками по образцу «инвертировать условие на короткий прыжок через `jal`»: `beq a0, a1, far` → `bne a0, a1, 8` + `jal zero, far`. Это ручной приём в исходнике, а не работа транслятора.

**`call` без PC-relative.** Раз §8 отказывается от pcrel, `call sym` раскрывается в `lui t0, %hi(sym)` + `jalr ra, %lo(sym)(t0)` с релокациями `R_RISCV_HI20` и `R_RISCV_LO12_I`. Тип `R_RISCV_CALL` не генерируется. Регистр `t0` при этом затирается — зафиксировать это в документации своего ассемблера, иначе однажды вы положите в `t0` что-то нужное перед вызовом.

**`lib.s` — общий рантайм.** В критерии готовности (§1) фигурирует `lib.o`, и это не случайность: `asm2` и `ld` разделяют заметный код — чтение файла в буфер, буферизованная запись, сравнение и хеширование имён, `itoa`, работа с таблицей символов, вывод ошибок. Вынести это в `lib.s` нужно на S3, до того как появится второй потребитель. Заодно `lib.o` даёт линкеру нетривиальный тест: два объектника с перекрёстными ссылками — именно то, ради чего S4 вообще существует.

**Секции в плоском выходе S2.** У `asm1` секций в файле нет, но директивы `.data` и `.bss` есть. Соглашение: `.data` укладывается сразу за `.text` в тот же сегмент, `.bss` не попадает в файл вовсе и размещается во втором сегменте по карте §5.3. Первый проход считает размеры обеих секций, второй — эмитирует.

---

## 8. Объектный формат и релокации

**Решение: настоящий ELF32 `ET_REL`, не свой формат.** Свой проще на день работы, но настоящий даёт то, что дороже: `llvm-readelf -r`, `llvm-objdump -dr` и `nm` начинают читать ваш выход, а системный `ld` может слинковать ваши `.o` — и результат сравнивается с работой вашего линкера. Отладка S3–S4 превращается из угадайки в сверку.

**Секции:** `.text`, `.data`, `.bss`, `.symtab`, `.strtab`, `.shstrtab`, `.rela.text`, `.rela.data`.

**`Elf32_Shdr` — 40 байт:** `sh_name`(4), `sh_type`(4), `sh_flags`(4), `sh_addr`(4), `sh_offset`(4), `sh_size`(4), `sh_link`(4), `sh_info`(4), `sh_addralign`(4), `sh_entsize`(4). Значения, которые обязаны быть именно такими:

| Секция | `sh_type` | `sh_flags` | `sh_link` | `sh_info` | `align` | `entsize` |
|---|---|---|---|---|---|---|
| индекс 0 | `SHT_NULL` (0) | все поля нулевые | | | | |
| `.text` | `PROGBITS` (1) | `ALLOC\|EXECINSTR` (6) | 0 | 0 | 4 | 0 |
| `.data` | `PROGBITS` (1) | `ALLOC\|WRITE` (3) | 0 | 0 | 4 | 0 |
| `.bss` | `NOBITS` (8) | `ALLOC\|WRITE` (3) | 0 | 0 | 4 | 0 |
| `.symtab` | `SYMTAB` (2) | 0 | индекс `.strtab` | индекс первого `GLOBAL` | 4 | 16 |
| `.strtab` | `STRTAB` (3) | 0 | 0 | 0 | 1 | 0 |
| `.shstrtab` | `STRTAB` (3) | 0 | 0 | 0 | 1 | 0 |
| `.rela.text` | `RELA` (4) | 0 | индекс `.symtab` | индекс `.text` | 4 | 12 |
| `.rela.data` | `RELA` (4) | 0 | индекс `.symtab` | индекс `.data` | 4 | 12 |

Четыре инварианта, на которых спотыкаются все, кто пишет ELF впервые:

1. **Секция с индексом 0 обязана существовать** и быть целиком нулевой; `e_shnum` считает её вместе со всеми.
2. **Символ с индексом 0 в `.symtab` обязан быть нулевым**, а первый байт `.strtab` — `\0`. Пустое имя должно указывать в него.
3. **`.bss` имеет тип `NOBITS`**: она не занимает места в файле, но `sh_offset` всё равно заполняется — значением, на котором она «была бы».
4. Для `ET_REL`: `e_phoff = 0`, `e_phnum = 0`, `e_shoff ≠ 0`, `e_shstrndx` = индекс `.shstrtab`. Программных заголовков у объектника нет.

**`Elf32_Sym` — 16 байт:**
```
st_name  4   смещение в .strtab
st_value 4   смещение внутри секции
st_size  4   можно 0
st_info  1   (bind << 4) | type
st_other 1   0
st_shndx 2   индекс секции, либо SHN_UNDEF = 0
```
Из привязок нужны `LOCAL`(0) и `GLOBAL`(1), из типов — `NOTYPE`(0), `OBJECT`(1), `FUNC`(2), `SECTION`(3).

**Требование ELF:** все `LOCAL`-символы обязаны идти в `.symtab` **перед** всеми `GLOBAL`, а `sh_info` секции `.symtab` равен индексу первого `GLOBAL`. Нарушение — и чужие инструменты откажутся читать файл.

**`Elf32_Rela` — 12 байт:** `r_offset`(4), `r_info`(4) = `(sym << 8) | type`, `r_addend`(4).

**Минимальный набор типов релокаций:**

| Тип | № | Где применяется |
|---|---|---|
| `R_RISCV_32` | 1 | абсолютное слово в данных |
| `R_RISCV_BRANCH` | 16 | B-формат, ±4 КиБ |
| `R_RISCV_JAL` | 17 | J-формат, ±1 МиБ |
| `R_RISCV_HI20` | 26 | верхние 20 бит абсолютного адреса в `lui` |
| `R_RISCV_LO12_I` | 27 | нижние 12 бит в I-формате |
| `R_RISCV_LO12_S` | 28 | нижние 12 бит в S-формате |

**Сознательный отказ от PC-relative.** Типы `R_RISCV_PCREL_HI20`/`PCREL_LO12` имеют известную особенность: `PCREL_LO12` ссылается не на целевой символ, а на *метку инструкции `auipc`*, что требует дополнительного прохода. Поскольку вы собираете статический `ET_EXEC` по фиксированному адресу, PIC не нужен — используйте абсолютные `HI20`/`LO12` и не трогайте pcrel вообще. `R_RISCV_RELAX` (51) в выходе не генерировать, во входе игнорировать.

---

## 9. Вехи

| Веха | Что сделано | Критерий приёмки |
|---|---|---|
| **M0** | ELF-заголовок 84 байта + `li a0,42; li a7,93; ecall`, набитые в hex-редакторе | `qemu-riscv32 ./m0; echo $?` печатает `42` |
| **M0.5** | Два `PT_LOAD` по карте §5.3, чтение `argc`/`argv[1]` со стека, запись и чтение байта в bss | `exit(argc)` даёт число аргументов; байт, записанный по `0x18000`, читается обратно |
| **M1** | Тот же ELF + вывод строки через `write` | На stdout появляется `hello` |
| **M2** | `hex0` работает | `hex0` собирает тестовый hex в M1 и результат идентичен |
| **M3** | `hex0` самособирается | `hex0(hex0.hex) == hex0` побайтово |
| **M4** | `hex2` работает | `hex2(hex2.hex2) == hex2` побайтово |
| **M5** | `asm1` собирает нетривиальную программу | Работающий `cat` на своём ассемблере |
| **M6** | `asm2` выдаёт `.o` | `llvm-readelf -a` читает без ошибок; системный `ld` линкует ваши `.o` в работающий бинарник |
| **M7** | `ld` работает | Ваш `ld` из тех же `.o` даёт бинарник, ведущий себя идентично |
| **M8** | Фикспоинт | `asm2` и `ld` пересобирают себя байт-в-байт |

**M0 — самая важная веха.** Пока `echo $?` не показал 42, нет смысла писать `hex0`: вы будете отлаживать одновременно заголовок и логику и не поймёте, где ошибка. Двенадцать байт кода:

```
13 05 A0 02    addi a0, zero, 42
93 08 D0 05    addi a7, zero, 93
73 00 00 00    ecall
```

**M0.5 не менее важна, и её легко пропустить.** M0 доказывает, что заголовок правильный. M0.5 доказывает, что правильна **карта памяти** — а именно на ней держатся все вручную вписанные константы S0 и S1. Три вещи, которые нужно проверить до того, как вы напишете первую строчку `hex0`, потому что потом их отказ будет выглядеть как «ошибка в логике парсера»:

1. Второй `PT_LOAD` действительно даёт обнулённую память по `0x18000` — записать байт, прочитать обратно, вернуть его кодом выхода.
2. Стек разложен как обещано — `exit(argc)` при трёх аргументах даёт 3, а строка по `[sp+8]` печатается через `write`.
3. Объявленный `p_filesz` не завышен сверх границы страницы — иначе программа умрёт по SIGBUS (код выхода 135) в момент первого обращения к дальней странице, и никакого сообщения вы не увидите.

---

## 10. Отладка и верификация

| Инструмент | Применение |
|---|---|
| `qemu-riscv32 -strace ./prog` | трассировка syscall — заменяет `strace` |
| `qemu-riscv32 -d in_asm ./prog` | лог выполненных инструкций |
| `qemu-riscv32 -g 1234 ./prog` + `gdb` | пошаговая отладка; в gdb `set architecture riscv:rv32`, `target remote :1234` |
| `llvm-objdump -d prog` | дизассемблирование — сверка того, что вы *хотели* закодировать, с тем, что получилось |
| `llvm-readelf -a prog` | проверка заголовков, секций, символов, релокаций |
| `cmp a b` | проверка фикспоинта |

`llvm-objdump` определяет архитектуру по `e_machine` и не требует установки кросс-binutils.

**Регрессионные тесты.** Каждая ступень с самого начала сопровождается набором `.hex`/`.s` файлов с ожидаемым выходом. При достижении каждой следующей вехи весь набор прогоняется заново. Формат простой: каталог `tests/`, скрипт-раннер разрешён (он не участвует в трансляции).

**Правило перепроверки:** новая ступень обязана заново собрать все предыдущие ступени и дать идентичные бинарники. Это ловит регрессии мгновенно.

---

## 11. Регламент работы с агентом

Проект учебный, поэтому роль помощника ограничивается явно.

**Агент может:**
- Объяснять форматы, спецификации, устройство ELF, семантику инструкций.
- Проверять уже посчитанную вами кодировку и указывать, в каком поле ошибка.
- Показывать *чужой* эталон для сравнения (`llvm-objdump` того, что сгенерировал системный ассемблер).
- Писать тесты, раннеры, вспомогательные заметки.
- Задавать наводящие вопросы при отладке.

**Агент не должен:**
- Выдавать готовые байты, которые вам предстоит набить, без вашей предварительной попытки.
- Писать исходный код любой ступени.
- Отвечать «здесь ошибка в байте 3», не объяснив, как вы могли бы найти это сами.

**Формат обращения при отладке:** сначала ваша гипотеза, потом вопрос. «Я закодировал `bne a0, zero, +8` как `0x00051463`, ожидаю переход через две инструкции, но qemu уходит не туда — где я ошибся в раскладке B-формата?» Это работает; «почему не работает» — нет.

---

## 12. Известные риски

| Риск | Митигация |
|---|---|
| RV32 user-mode в QEMU менее обкатан, чем RV64 | смоук-тест на M0, готовность переключиться на RV64I |
| Разный порядок полей в `Elf32_Phdr` и `Elf64_Phdr` | сверять с `llvm-readelf -l` на M0 |
| Раскладка immediate в B/J-форматах | отдельный тест на каждый формат: собрать переход, проверить `llvm-objdump` |
| Коррекция `%hi` при отрицательном `%lo` | тест с адресом, у которого бит 11 младшей части равен 1 |
| Порядок LOCAL/GLOBAL в `.symtab` | `llvm-readelf -s` на M6 |
| Отсутствие `open` в generic-таблице syscall | использовать только `openat` с `AT_FDCWD` |
| Рост смещений при правке S0 | резерв из `nop`, данные в bss по фиксированным адресам |
| `p_filesz` завышен сверх границы страницы файла | SIGBUS без сообщения (код 135); правило §5.2, проверка на M0.5 |
| Рост кода за 4 КБ сдвигает адреса данных | отдельный `PT_LOAD` под bss с `filesz = 0` (§5.2) |
| Потерян бинарник ступени — пересобрать нечем | git с первого дня, каталог `golden/`, coding sheet как второй исходник (§13) |
| Молчаливое переполнение таблиц в bss | явный потолок у каждой таблицы, `exit(2)` с сообщением |
| Длина псевдоинструкции зависит от forward-символа | `la`/`call` всегда 2 инструкции, relaxation запрещён (§7.5) |
| Ветвление дальше ±4 КиБ при `.text` > 4 КБ | ошибка ассемблера + ручная инверсия через `jal` (§7.5) |
| Отсутствие диагностики на S2+ | номер строки и код ошибки в stderr — обязательны (§7.5) |

---

## 13. Артефакты и bootstrap-ловушка

У проекта есть свойство, которого нет у обычных: **бинарники здесь — это исходники**. Потеряв рабочий `hex0`, вы не сможете собрать его ничем — всё остальное собирается им. Восстановление возможно только повторной ручной набивкой в hex-редакторе. Отсюда режим хранения.

- **git с первого дня**, и бинарники ступеней **коммитятся в репозиторий**. Обычное правило «артефакты сборки в git не кладут» здесь не действует: это не артефакты, это корень доверия.
- **Каталог `golden/`** — эталонные копии всех достигнутых ступеней. Правило перепроверки из §10 сверяется именно с ними: новая ступень пересобирает все предыдущие, `cmp` с эталоном.
- **Тег на каждый фикспоинт**: `s0-selfhost`, `s1-selfhost`, `s3-selfhost`. К этим точкам вы будете возвращаться при регрессиях.
- **Copy вне рабочего каталога.** Одна `rm -rf` в неудачный момент стоит недели ручной набивки.
- **Coding sheet ступени S0 — это её второй исходник.** Формат `адрес | 4 байта | мнемоника | комментарий` (§6.6) полностью восстанавливает бинарник. Держите его в git рядом с `hex0.hex`.

Структура каталогов:

```
s0/   hex0.hex   hex0        hex0.sheet.md
s1/   hex2.hex   hex2
s2/   asm1.hex2  asm1
s3/   asm2.s     asm2        lib.s  lib.o
s4/   ld.s       ld
golden/                      эталоны всех ступеней
tests/                       регрессия: вход, ожидаемый выход, ожидаемый код возврата
docs/                        эта спецификация, карта памяти, таблицы кодировок
```

---

## 14. Журнал изменений

**1.1** — правки после смоук-теста окружения и четырёх пробных ELF.

| Что изменено | Причина |
|---|---|
| §2.1: зафиксирован результат смоук-теста, решение остаться на RV32I | окружение проверено, резервный план RV64 больше не висит открытым вопросом |
| §2.1: требование поставить интерактивный hex-редактор | в системе есть только `xxd`, запрещённый §3; без редактора ступень S0 нечем набивать |
| §5.2: отменён совет «добить файл нулями до 4096» | добивка требует ~7 тысяч hex-цифр в `hex0.hex` и убивает самосборку S0; хвост страницы и так обнулён ядром |
| §5.2: правило `p_filesz` ≤ округления размера файла до 4 КБ | завышение даёт SIGBUS без сообщения — проверено, код выхода 135 |
| §5.2: второй `PT_LOAD` под bss с `filesz = 0` | иначе адреса данных зависят от размера кода, а на них завязаны константы, вписанные в S0/S1 руками |
| §5.3: единая карта памяти с раскладкой, размерами и лимитами | старая раскладка на 512-байтовых буферах не выдерживает самосборки `asm2` |
| §7.5: CLI, коды выхода, диагностика с номером строки | без номера строки отладка 10 КБ исходника на своём ассемблере нереальна |
| §7.5: правило фиксированной длины `la`/`call`, запрет relaxation | двухпроходная схема ломается, если длина инструкции зависит от forward-символа |
| §7.5: раскрытие `call` без pcrel, назначение `lib.s` | `lib.o` фигурировал в критерии готовности §1, но нигде не был определён |
| §8: раскладка `Elf32_Shdr` и четыре инварианта ELF | S3 обязан писать заголовки секций, а спецификация давала только `Sym` и `Rela` |
| §9: веха M0.5 | карта памяти и раскладка стека должны быть проверены до написания `hex0`, иначе их отказ маскируется под ошибку парсера |
| §13: bootstrap-ловушка, git, `golden/` | потеря бинарника ступени стоит недели ручной работы, а обычные правила гигиены репозитория тут вредны |

---

*Спецификация подлежит уточнению по ходу проекта. Все изменения фиксировать здесь же с указанием причины — история решений полезнее, чем чистовик.*
